{{ form_start(form, {'attr': {'class' : 'row'}} ) }}
    <div class="col-md-8">
        {{ form_row(form.name)}}

        <button class="btn btn-outline-black mx-1 my-3">
            {{ button_label|default('Créer') }}&nbsp;&nbsp;<i class="bi bi-check-lg"></i>
        </button>
    </div>
{{ form_end(form) }}
