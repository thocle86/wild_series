<form
    style="width:initial;max-width:initial"
    class="m-0 p-0"
    action="{{ path('program_show', {'id': program.id}) }}">
    <button type="submit" class="btn btn-outline-info m-1">
        <i class="bi bi-info-square"></i>
    </button>
</form>
