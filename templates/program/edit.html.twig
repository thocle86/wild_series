{% extends 'base.html.twig' %}

{% block title %}Éditer la série{% endblock %}

{% block body %}

    <h1 class="m-3">
        Éditer la série
    </h1>

    <div class="row">
        <div class="col-9 d-flex justify-content-start my-3">
            {{ include('program/_button_return.html.twig') }}
        </div>
        <div class="col-3 d-flex justify-content-end my-3">
            {{ include('program/_button_show.html.twig') }}
        </div>
    </div>

    <div class="row d-flex justify-content-center m-3">
        {{ include('program/_form.html.twig', {'button_label': 'Éditer'}) }}
    </div>

{% endblock %}
