<form
    style="width:initial;max-width:initial"
    class="m-0 p-0"
    action="{{ path('program_edit', {'id': program.id}) }}">
    <button type="submit" class="btn btn-outline-primary m-1">
        <i class="bi bi-pencil-square"></i>
    </button>
</form>
